//armazenando o objeto
const pessoa = {
  //declarando a propriedade/chave e após os : valor que será armazenado.
  //propriedade: valor
  personalidade: ['personalide1', 'personalide2'],
  areaAtuacao: 'Biologa'
}

//consultando no objeto pessoa a propriedade personalizada no índice 0.
pessoa.personalidade[0];

//mostrando no console e/ou terminal os valores que contém dentro objeto do pessoa na propriedade personalidade na posição 0.
console.log("Personalidade1: ", pessoa.personalidade[0]);

// armazenando o objeto na variável meuObjeto
const meuObjeto = {
  tarefas: ['Tomar café', 'Estudar']
}

//criando um objeto que contém propriedades e valores
const cliente1 = {
  nome: 'Maria',
  idade: 30,
  profissão: ['Escritora', 'Desenvolvedora', 'Médica']
}

//criando um objeto de forma nativa
const objetoNativo = new Object()






